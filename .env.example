# Apollo API Configuration
APOLLO_API_KEY=7TtLS9kiLP5dlE76kJdeUQ

# Database Configuration
DB_PATH=apollo_truth.db
# DATABASE_URL=postgresql://... (Supabase; optional, for production / Streamlit Cloud)

# Apollo API Settings
APOLLO_BASE_URL=https://api.apollo.io/api/v1
APOLLO_BATCH_SIZE=25
APOLLO_TIMEOUT=30
APOLLO_MAX_RETRIES=5
APOLLO_INITIAL_BACKOFF=1.0
APOLLO_MAX_BACKOFF=60.0

# Processing Configuration
MAX_FILE_SIZE_MB=50
EXCEL_CHUNK_SIZE=1000
ENABLE_PEOPLE_ENRICHMENT=true
ENABLE_COMPANY_ENRICHMENT=true

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=apollo_pipeline.log
LOG_TO_CONSOLE=true

# UI Configuration
STREAMLIT_PAGE_TITLE=Apollo Enrichment Pipeline
PAGE_SIZE_DEFAULT=50
ENABLE_EXPORT=true

# Apify (required only for Scrape website fallback; get token at https://console.apify.com/settings/integrations)
# APIFY_API_TOKEN=your_apify_token_here
# APIFY_AI_EXTRACTOR_ACTOR_ID=apify/ai-web-agent
# APIFY_RUN_TIMEOUT_SECS=900  # Increase for long pages (default 300)
# APIFY_EXTRACTION_PROMPT=Important: Extract data first. Do not take screenshots before extracting. Extract all person names...
# APIFY_WAIT_FOR_DYNAMIC_CONTENT=true  # Wait for JS-rendered content before extracting
# APIFY_PAGE_WAIT_SECS=5  # Hint for agent to wait N seconds for dynamic content

# HTML-first extraction (try fetch + LLM before Apify; uses fewer resources)
# ENABLE_HTML_FIRST_EXTRACTION=true  # Set false to use Apify only (default true)
# HTML_FETCH_TIMEOUT_SECS=30  # Timeout for fetching page HTML (default 30)
# MAX_HTML_CHARS=120000  # Truncate HTML to this many chars for LLM (default 120000)
# OPENAI_EXTRACTION_MODEL=gpt-4o-mini  # Model for HTML extraction (default gpt-4o-mini)
